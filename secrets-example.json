# Example Brickmaster2 Secrets file.
{
  # WIFI info is required if running on a CircuitPython board (like a Metro M4 Airlift)
  # This gets ignored if you're on a general-purpose system, since then the base OS should
  # handle the network connectivity.
  "SSID": <SSID of the network>,
  "passphrase": <Password to connect>,
  # MQTT connectivity. Does NOT support TLS at this point.
  "broker": <IP or Hostname of the MQTT Broker>
  "port": <Port for MQTT broker. Will default to 1883 if not included.>
  "username": <MQTT Username>
  "password": <MQTT Password>
}